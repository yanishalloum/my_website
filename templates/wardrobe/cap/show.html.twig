{% extends 'base.html.twig' %}

{% block title %}Cap{% endblock %}

{% block main %}
    <h1>Cap</h1>

    <table class="table">
        <tbody>
        <tr>
            <th>Id</th>
            <td>{{ cap.id }}</td>
        </tr>
        <tr>
            <th>Name</th>
            <td>{{ cap.name }}</td>
        </tr>
        <tr>
            <th>Brand</th>
            <td>{{ cap.brand }}</td>
        </tr>
        <tr>
            <th>Year</th>
            <td>{{ cap.year }}</td>
        </tr>
        </tbody>
    </table>

    <a href="{{ path('app_wardrobe_show', { 'id': wardrobe.id}) }}">back to wardrobe</a>

    <a href="{{ path('app_cap_edit', {'id': cap.id}) }}">edit</a>

    {{ include('cap/_delete_form.html.twig') }}
{% endblock %}
